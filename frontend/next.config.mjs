/** @type {import('next').NextConfig} */
// Template aplikasi baru — semua alias & redirect lama ke halaman fitur
// (laporanrusak, aset, dll.) sudah dihapus bersama halaman tersebut.
const nextConfig = {};

export default nextConfig;
